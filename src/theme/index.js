import { extendTheme } from '@chakra-ui/react'

const theme = extendTheme({
    colors: {
        primay: '#ff0000',
        brand: {
            100: '#f7fafc',
            900: '#1a202c',
        },
    },
})

export default theme
