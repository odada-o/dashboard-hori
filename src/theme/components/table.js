import { mode } from '@chakra-ui/theme-tools'
export const TableStyles = {
    components: {
        Table: {
            baseStyle: {
                thead: {
                    th: {
                        background: 'red',
                        borderBottomWidth: '2px',
                    },
                },
            },
        },
    },
}
